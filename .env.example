# plan_dispatch_v1.10.md §24.4：dispatch 不讀 cwd 的 .env（cwd 是被審專案，讀了會把
# 該專案的整份 .env 載入行程）。把這份檔案複製到 $DISPATCH_HOME/.env（預設
# ~/.config/dowafu/.env；可用 DISPATCH_HOME 環境變數覆寫），或直接以 ambient
# process.env 提供（ambient 優先於 $DISPATCH_HOME/.env）。這份 repo 根目錄下的
# .env.example 只是範本，不是 dispatch 實際讀取的位置。
DEEPSEEK_API_KEY=
GEMINI_API_KEY=
OPENAI_API_KEY=
ANTHROPIC_API_KEY=